/**
 * 주어진 문자열의 문자 수를 반환합니다.
 *
 * @param {string} str - 문자 수를 세고자 하는 문자열.
 * @returns {number} - 문자열의 문자 수.
 */
export function getCharCount(str) {}

/**
 * 주어진 단의 구구단을 문자열로 반환합니다.
 *
 * @param {number} dan - 구구단을 출력할 단.
 * @returns {string} - 주어진 단의 구구단 결과를 콤마(,)로 구분한 문자열.
 */
export function gugudan(dan) {}

/**
 * 주어진 문자열에서 특정 문자의 개수를 반환합니다.
 *
 * @param {string} str - 문자를 셀 문자열.
 * @param {string} char - 개수를 셀 특정 문자.
 * @returns {number} - 문자열에서 특정 문자의 개수.
 */
export function countChar(str, char) {}

/**
 * 문자열에서 특정 문자를 다른 문자로 대체하여 반환합니다.
 *
 * @param {string} str - 대체 작업을 수행할 문자열.
 * @param {string} from - 대체할 문자.
 * @param {string} to - 대체된 문자.
 * @returns {string} - 특정 문자가 대체된 새로운 문자열.
 */
export function replaceFromTo(str, from, to) {}
