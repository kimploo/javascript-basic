/**
 * 주어진 객체의 특정 키에 값을 설정하고 업데이트된 객체를 반환합니다.
 *
 * @param {Object} obj - 값을 설정할 객체
 * @param {string} key - 값을 설정할 키
 * @param {*} value - 설정할 값
 * @returns {Object} 업데이트된 객체
 */
export function setPropertyValue(obj, key, value) {
  return obj;
}

/**
 * 주어진 객체에서 특정 키의 값을 가져옵니다.
 *
 * @param {Object} obj - 값을 가져올 객체
 * @param {string} key - 값을 가져올 키
 * @returns {*} 해당 키의 값
 */
export function getPropertyValue(obj, key) {}

/**
 * 주어진 객체에서 특정 키의 속성을 제거하고 업데이트된 객체를 반환합니다.
 *
 * @param {Object} obj - 속성을 제거할 객체
 * @param {string} key - 제거할 속성의 키
 * @returns {Object} 업데이트된 객체
 */
export function removeProperty(obj, key) {
  return obj;
}

/**
 * 주어진 객체의 모든 속성과 값을 문자열로 반환합니다.
 *
 * @param {Object} obj - 속성을 출력할 객체
 * @returns {string} 객체의 모든 속성과 값을 포함한 문자열
 */
export function printAllProperties(obj) {
  let result = "";

  return result;
}

/**
 * 주어진 두 객체를 병합하고 병합된 객체를 반환합니다.
 * obj2에 있는 속성이 obj1에 존재하지 않을 경우에만 obj1에 추가됩니다.
 *
 * @param {Object} obj1 - 기본 객체
 * @param {Object} obj2 - 추가 속성을 가진 객체
 * @returns {Object} 병합된 객체
 */
export function extend(obj1, obj2) {
  return obj1;
}
